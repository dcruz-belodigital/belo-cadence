{{--
    Lighter than an ordinary border. A menu's rows are already separated by the space
    around them, so the rule only has to hint at the grouping; at full strength it drew
    more attention than the entries it divides.

    The tint is taken from the theme's own border colour, so it stays right whichever
    theme is being worn.
--}}
<div class="h-px bg-border/60" role="separator"></div>
